services:
  - type: web
    name: bharat-construction
    runtime: static
    buildCommand: "./mvnw clean install"
    startCommand: "java -jar target/*.jar"
    envVars:
      - key: JAVA_VERSION
        value: 17

